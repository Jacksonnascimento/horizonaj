version: '3.8'

services:
  site-horizon:
    image: ghcr.io/jacksonnascimento/horizonaj:latest
    container_name: site-horizon
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:80" 
    networks:
      - site-network

networks:
  site-network:
    driver: bridge